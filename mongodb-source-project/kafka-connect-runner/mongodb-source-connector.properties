name=mongodb-source-connector
connector.class=com.yourcompany.mongodbsource.MongoSourceConnector
tasks.max=1
bootstrap.servers=kafka:9092

# You can change this to any Kafka topic
topic=test-mongo-topic

# Custom connector settings — you’ll use these in your Java code later
mongodb.uri=mongodb://mongodb:27017
mongodb.database=test
mongodb.collection=messages

